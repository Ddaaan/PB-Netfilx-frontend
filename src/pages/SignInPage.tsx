export default function SignInPage() {
    return (
        <div style={{ padding: 24 }}>
            <h1>/signin</h1>
            <p>다음 단계에서 로그인/회원가입 전환 애니메이션 + localStorage 인증 구현할 예정</p>
        </div>
    );
}
